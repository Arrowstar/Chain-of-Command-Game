\n@@ -371,7 +371,7 @@\n                 {campaign.drydockMarket?.techOffer && (() => {\n                   const tech = getTechById(campaign.drydockMarket!.techOffer!);\n                   if (!tech) return null;\n-                  const techCost = discountedCost(tech.rarity === 'rare' ? 45 : 30);\n+                  const techCost = discountedCost(tech.rarity === 'rare' ? 50 : tech.rarity === 'uncommon' ? 40 : 30);\n                   const canAffordTech = campaign.requisitionPoints >= techCost;\n                   return (\n                     <div key={tech.id} className=\"panel\" style={{ ...panelStyle, opacity: canAffordTech ? 1 : 0.6, borderLeft: '3px solid var(--color-holo-cyan)' }}>\n
\n@@ -1,4 +1,4 @@\n-import React, { useState, useEffect, useRef } from 'react';\n+import React, { useState, useEffect } from 'react';\n import { SHIP_CHASSIS, getChassisById } from '../../data/shipChassis';\n import { OFFICERS } from '../../data/officers';\n import { WEAPONS, TAG_DESCRIPTIONS, TAG_LABELS, getPurchasableWeapons } from '../../data/weapons';\n@@ -5,6 +5,6 @@\n import { ADVERSARIES } from '../../data/adversaries';\n import { useGameStore, type GameInitConfig } from '../../store/useGameStore';\n-import { computeDpBreakdown } from '../../engine/dpCost';\n+import { computeDpBreakdown, DP_BUDGET } from '../../engine/dpCost';\n<truncated 932 bytes>"}
{"step_index":477,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -48,7 +48,7 @@\n   return `M ${pts[0]} L ${pts[1]} L ${pts[2]} L ${pts[3]} L ${pts[4]} L ${pts[5]} Z`;\n }\n \n-function ShipLoadoutPreview({ chassis, selectedWeaponIds, hoveredWeaponId }: { chassis: any; selectedWeaponIds: string[]; hoveredWeaponId?: string | null }) {\n+function ShipLoadoutPreview({ chassis, selectedWeaponIds }: { chassis: any, selectedWeaponIds: string[] }) {\n   const cx = 150;\n   const cy = 150;\n   const hexSize = 16; \n
\n@@ -77,7 +77,6 @@\n         {/* Highlighted Weapon Hexes */}\n         {weapons.map((w, index) => {\n           const color = WEAPON_COLORS[index % WEAPON_COLORS.length];\n-          const isHov = hoveredWeaponId === w.id;\n           const maxRange = w.rangeMax === Infinity ? 6 : w.rangeMax;\n           const minRange = w.rangeMin || 0;\n           \n
\n@@ -91,15 +91,14 @@\n           const d = coveredHexes.map(h => getHexGridPath(cx, cy, hexSize, h.q, h.r)).join(' ');\n \n           return (\n-            <path\n-              key={`${w.id}-hexes`}\n+            <path \n+              key={`${w.id}-hexes`} \n               d={d}\n-              fill={color}\n-              opacity={isHov ? 0.55 : 0.25}\n+              fill={color} \n+              opacity=\"0.3\"\n               stroke={color}\n-              strokeWidth={isHov ? 2.5 : 1.5}\n+              strokeWidth=\"1.5\"\n               strokeLinejoin=\"round\"\n-              style={{ transition: 'opacity 0.2s, stroke-width 0.2s' }}\n             />\n           );\n         })}\n
\n@@ -137,30 +137,9 @@\n           strokeWidth=\"1.5\"\n         />\n       </svg>\n-      <div className=\"mono\" style={{ fontSize: '0.65rem', color: 'var(--color-text-dim)', marginTop: 'var(--space-sm)' }}>SHIELD SECTORS &amp; WEAPON ARCS</div>\n-      {weapons.length > 0 && (() => {\n-        const avgDie: Record<string,number> = { d4:2.5, d6:3.5, d8:4.5, d10:5.5, d12:6.5, d20:10.5 };\n-        const totalAvg = weapons.reduce((s,w) => s + w.volleyPool.reduce((a:number,d:string) => a+(avgDie[d]||0), 0), 0);\n-        const abbr: Record<string,string> = { fore:'F', foreStarboard:'F-S', aftStarboard:'A-S', aft:'A', aftPort:'A-P', forePort:'F-P' };\n-        return (\n<truncated 1988 bytes>"}
{"step_index":481,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -178,49 +178,10 @@\n   const campaignBudget = isCampaignSetup ? dpBudgetMap[campaignDifficulty] : 100;\n \n   useEffect(() => {\n-    if (isCampaignSetup) return;\n+    if (isCampaignSetup) return; // Campaign names are handled separately\n     const chassisName = selectedChassisId ? getChassisById(selectedChassisId)?.name || 'Vanguard' : 'Ship';\n     setCustomShipName(`ISS ${chassisName}${currentPlayerIndex > 0 ? ` ${currentPlayerIndex + 1}` : ''}`);\n   }, [currentPlayerIndex, selectedChassisId, isCampaignSetup]);\n-\n<truncated 6161 bytes>"}
{"step_index":482,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -413,24 +413,7 @@\n   // Require at least 1 weapon; subsystems are optional (any number from 0 to max slots).\n   const hasAllModules = activeChassis && selectedWeapons.length >= 1;\n \n-  // ── Keyboard shortcuts ─────────────────────────────────────────\n-  useEffect(() => {\n-    const handler = (e: KeyboardEvent) => {\n-      if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;\n-      if (e.key === 'Enter') {\n-        if (step === 1 && (isCampaignSetup ? true : (!!selectedChassisId && !!customShipName.trim()))) { setStepDirection('forward'); setStep(2); }\n<truncated 1796 bytes>"}
{"step_index":483,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -448,7 +448,7 @@\n \n   return (\n     <div className=\"panel\" style={{ width: '100vw', height: '100vh', display: 'flex', flexDirection: 'column', borderRadius: 0, border: 'none' }}>\n-      {/* Ã¢â€â‚¬Ã¢â€â‚¬ Top Bar Ã¢â€â‚¬Ã¢â€â‚¬ */}\n+      {/* ── Top Bar ── */}\n       <div style={{ padding: 'var(--space-md)', borderBottom: '1px solid var(--color-border)', display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>\n         <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-md)' }}>\n           {onCancel && <button className=\"btn\" onClick={onCancel} style={{ padding: '4px 8px', fontSize: '0.8rem' }}>&larr; BACK</button>}\n<truncated 245 bytes>"}
{"step_index":484,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -491,7 +491,7 @@\n                   }}\n                   title={isClickable ? `Go back to ${stepLabels[s]}` : undefined}\n                 >\n-                  {isPast ? 'Ã¢Å“â€œ ' : isActive ? 'Ã¢â€“Â¶ ' : ''}{s}. {stepLabels[s]}\n+                  {isPast ? '✓ ' : isActive ? '▶ ' : ''}{s}. {stepLabels[s]}\n                 </button>\n                 {idx < arr.length - 1 && (\n                   <span style={{ color: 'var(--color-text-dim)', fontSize: '0.7rem' }}>/</span>\n
\n@@ -502,7 +502,7 @@\n         </div>\n       </div>\n \n-      {/* Ã¢â€â‚¬Ã¢â€â‚¬ Player Progress Tracker Ã¢â€â‚¬Ã¢â€â‚¬ */}\n+      {/* ── Player Progress Tracker ── */}\n       {totalPlayers > 1 && (\n         <div style={{ display: 'flex', borderBottom: '1px solid var(--color-border)', background: 'rgba(0,0,0,0.3)' }}>\n           {Array.from({ length: totalPlayers }).map((_, idx) => {\n
\n@@ -524,7 +524,7 @@\n                 }}\n               >\n                 <div className=\"mono\" style={{ fontSize: '0.65rem', color: isDone ? 'var(--color-holo-green)' : isActive ? 'var(--color-holo-cyan)' : 'var(--color-text-dim)' }}>\n-                  {isDone ? 'Ã¢Å“â€œ ' : isActive ? 'Ã¢â€“Â¶ ' : ''}{`PLAYER ${idx + 1}`}\n+                  {isDone ? '✓ ' : isActive ? '▶ ' : ''}{`PLAYER ${idx + 1}`}\n                 </div>\n                 <div className=\"mono\" style={{ fontSize: '0.8rem', color: isDone ? 'var(--color-holo-green)' : isActive ? 'var(--color-text-bright)' : 'var(--color-text-dim)', fontWeight: isActive ? 'bold' : 'normal' }}>\n                   {shipNameForIdx}\n<truncated 245 bytes>"}
{"step_index":487,"source":"USER_EXPLICIT","type":"CODE_ACTION","status":"DONE","created_at":"2026-04-27T21:06:15Z","content":"The following changes were made by the USER to: c:\\Users\\Adam\\Dropbox\\Documents\\homework\\Personal Projects\\Chain of Command Game\\app\\src\\components\\setup\\FleetBuilder.tsx. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.\n[diff_block_start]\n@@ -535,7 +535,7 @@\n         </div>\n       )}\n \n-      {/* Ã¢â€â‚¬Ã¢â€â‚¬ Active Player Banner (only shown during officer/module steps) Ã¢â€â‚¬Ã¢â€â‚¬ */}\n+      {/* ── Active Player Banner (only shown during officer/module steps) ── */}\n       {step >= 2 && (\n         <div style={{ padding: '10px var(--space-lg)', background: 'rgba(0, 204, 255, 0.05)', borderBottom: '1px solid var(--color-border)', display: 'flex', alignItems: 'center', gap: 'var(--space-md)' }}>\n           <span className=\"label\" style={{ color: 'var(--color-holo-cyan)' }}>NOW CONFIGURING:</span>\n
\n@@ -549,7 +549,7 @@\n         </div>\n       )}\n \n-      {/* Ã¢â€â‚¬Ã¢â€â‚¬ DP Budget Tracker (campaign Steps 2 & 3 only) Ã¢â€â‚¬Ã¢â€â‚¬ */}\n+      {/* ── DP Budget Tracker (campaign Steps 2 & 3 only) ── */}\n       {isCampaignSetup && step >= 2 && dpBreakdown && (() => {\n         const pct = Math.min((dpBreakdown.total / campaignBudget) * 100, 100);\n         const isOver = dpBreakdown.isOverBudget;\n
\n@@ -601,7 +601,7 @@\n                   className=\"mono dp-over-budget-badge\"\n                   style={{ fontSize: '0.7rem', padding: '1px 6px', borderRadius: '3px', flexShrink: 0, border: '1px solid' }}\n                 >\n-                  Ã¢Å¡Â  OVER BUDGET\n+                  ⚠ OVER BUDGET\n                 </span>\n               )}\n             </div>\n